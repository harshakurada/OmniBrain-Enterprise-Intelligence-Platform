# Lightweight dependency set for the Streamlit frontend container (Module 7).
# frontend/app.py only talks to the backend over HTTP -- it never touches
# LangChain/LangGraph/OpenAI/SQLAlchemy/PDF-parsing directly, so it does not
# need the full backend requirements.txt, which keeps this image small and
# fast to build/start.
streamlit>=1.32.0
httpx>=0.27.0
python-dotenv>=1.0.1
